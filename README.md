# Command Line Task Tracker

A command line based personal Task Tracker

Features: CRUD operations, automatic custom ID generation, and persistent JSON storage.

What I Learned: Mention input validation handling and list comprehensions.

Currently learning SQL so I might migrate the data to an SQLite database in the future 
